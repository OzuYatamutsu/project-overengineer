{
  "name": "@project-overengineer/integration-tests",
  "version": "0.1.0",
  "description": "E2E integration tests for the OCR service",
  "scripts": {
    "test": "TS_NODE_TRANSPILE_ONLY=1 playwright test -c playwright.config.ts"
  },
  "dependencies": {
    "@playwright/test": "^1.59.1"
  },
  "devDependencies": {
    "@types/node": "^25.5.2"
  }
}
