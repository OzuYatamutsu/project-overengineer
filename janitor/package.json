{
  "name": "@project-overengineer/janitor",
  "version": "0.2.0",
  "description": "A service which cleans up completed and stalled jobs.",
  "main": "worker.ts",
  "scripts": {
    "dev": "tsx watch worker.ts",
    "build": "tsc",
    "start": "node dist/worker.js",
    "lint": "eslint *.ts",
    "test": "pnpm build && TS_NODE_TRANSPILE_ONLY=1 playwright test -c playwright.config.ts"
  },
  "dependencies": {
    "@project-overengineer/shared-lib": "workspace:*"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.10.1",
    "@typescript-eslint/eslint-plugin": "^8.37.0",
    "@typescript-eslint/parser": "^8.37.0",
    "eslint": "^9.39.1",
    "tsx": "^4.21.0",
    "typescript": "^5.8.3"
  }
}
