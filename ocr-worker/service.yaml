apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: ocr-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ocr-worker
  template:
    metadata:
      labels:
        app: ocr-worker
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: ocr-worker
                topologyKey: "kubernetes.io/hostname"
      containers:
        - name: ocr-worker
          image: ghcr.io/ozuyatamutsu/project-overengineer/ocr-worker:v4
          imagePullPolicy: Always
          env:
            - name: SENTINEL_HOST
              value: svc-redis-sentinel.default.svc.cluster.local
            - name: SENTINEL_PORT
              value: "26379"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://svc-alloy.monitoring-plane.svc.cluster.local:4317"
            - name: VAULT_HOST
              value: svc-vault.default.svc.cluster.local
            - name: VAULT_PORT
              value: "8200"
            - name: VAULT_RO_TOKEN
              valueFrom:
                secretKeyRef:
                  name: vault-token
                  key: token
            # REDIS_HOST, REDIS_PORT, and REDIS_PASSWORD (not specified here)
            # are pulled from vault in-cluster at runtime, and refreshed regularly.
            - name: REDIS_HOST
              value: svc-redis-master.default.svc.cluster.local
            - name: REDIS_PORT
              value: "6379"
          readinessProbe:
            httpGet:
              path: /healthz
              port: 3002
            initialDelaySeconds: 10
            timeoutSeconds: 5
            periodSeconds: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: 3002
            initialDelaySeconds: 30
            timeoutSeconds: 5
            periodSeconds: 30
            failureThreshold: 5
            successThreshold: 1
          ports:
            - containerPort: 3002
            - containerPort: 4000
          resources:
            requests:
              cpu: "40m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          securityContext:
            readOnlyRootFilesystem: true
        - name: ollama-ocr    # sidecar
          image: ghcr.io/ozuyatamutsu/project-overengineer/ollama-ocr:v4
          env:
          - name: OLLAMA_KEEP_ALIVE
            value: "-1"
          - name: OLLAMA_MAX_LOADED_MODELS
            value: "1"
          - name: OLLAMA_REQUEST_TIMEOUT
            value: "900s"
          readinessProbe:
            httpGet:
              path: /api/ps
              port: 11434
            initialDelaySeconds: 10
            timeoutSeconds: 5
            periodSeconds: 3
          livenessProbe:
            httpGet:
              path: /api/ps
              port: 11434
            initialDelaySeconds: 30
            timeoutSeconds: 5
            periodSeconds: 30
            failureThreshold: 5
            successThreshold: 1
          ports:
            - containerPort: 11434
          resources:
            requests:
              cpu: "1000m"
              memory: "3Gi"
            limits:
              cpu: "4000m"
              memory: "6Gi"
          volumeMounts:
          - mountPath: /dev/shm
            name: ollama-ocr-shm
          - mountPath: /.ollama
            name: ollama-ocr-data
          - mountPath: /tmp
            name: ollama-ocr-tmp
          securityContext:
            readOnlyRootFilesystem: true
      volumes:
        - name: ollama-ocr-shm
          emptyDir:
            medium: Memory
            sizeLimit: 1Gi
        - name: ollama-ocr-tmp
          emptyDir: {}
      securityContext:
        runAsUser: 1002  # ollama user
        runAsGroup: 1002  # ollama group
        fsGroup: 1002  # ollama group
        runAsNonRoot: true
  volumeClaimTemplates:
  - metadata:
      name: ollama-ocr-data
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 256Mi

---

apiVersion: v1
kind: Service
metadata:
  name: svc-ocr-worker
spec:
  selector:
    app: ocr-worker
  ports:
    - name: http
      port: 3002
      targetPort: 3002
    - name: metrics
      port: 4000
      targetPort: 4000
  type: ClusterIP
